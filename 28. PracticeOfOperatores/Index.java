public class Index {
  public static void main(String[] args) {

    // ? 1. Sum of firt N Natural Number (forLoop, n*(n+1)/2)
    // ? 2. Find Last Digit of A Number in Java (use of Modulo)
    // ? 3. Find nth term of an Arithmetic Progression in Java (use this formula: a
    // ? + (N - 1) * d)
    // ? 4. Find nth term of an Geometric Progression in Java (use this formula: a *
    // ? (int)(Math.pow(r,N - 1)))

    // ? Day before N days ( d-(n%7)) and + 7 if day is < 0
  }

}
